# 🧪 修复测试指南

**测试日期**: 2025年11月30日  
**测试目标**: 验证 sendMatchReminders 云函数修复

---

## ✅ 修复验证状态

### 代码检查
- ✅ `package.json` 格式正确
- ✅ 无编译错误
- ✅ Cron 表达式已修正为 5 位：`*/5 * * * *`
- ✅ 包名已改为：`send-match-reminders`

---

## 📋 测试步骤

### 第一步：部署云函数

#### 方法 1：使用微信开发者工具（推荐）

1. **打开微信开发者工具**
   - 确保已打开 CSFC 项目

2. **定位到云函数**
   - 在左侧文件树找到：`cloudfunctions/sendMatchReminders`

3. **右键部署**
   - 右键点击 `sendMatchReminders` 文件夹
   - 选择 **"上传并部署：云端安装依赖"**
   
4. **等待部署完成**
   ```
   上传中...
   ✅ 上传成功
   云端安装依赖...
   ✅ 部署成功
   ```

5. **查看部署结果**
   - 底部控制台会显示部署日志
   - 确认看到 "部署成功" 提示

#### 方法 2：使用命令行

```powershell
# 进入云函数目录
cd f:\CSFC\cloudfunctions\sendMatchReminders

# 安装依赖
npm install

# 返回项目根目录
cd ../..

# 使用微信开发者工具的命令行工具部署
# （需要先安装命令行工具）
```

---

### 第二步：验证云开发控制台配置

1. **打开云开发控制台**
   - 在微信开发者工具中
   - 点击顶部工具栏 "云开发" 按钮
   - 或访问：https://console.cloud.tencent.com/tcb

2. **进入云函数管理**
   - 左侧菜单 → "云函数"
   - 找到 `sendMatchReminders` 函数

3. **检查函数配置**
   - 点击函数名称进入详情
   - 查看 "触发器" 标签页

4. **验证定时器配置**
   
   **应该看到**：
   ```
   触发器类型: 定时触发器
   触发周期: */5 * * * *
   状态: 已启用 ✅
   ```
   
   **如果显示错误配置**（旧的7位）：
   ```
   触发周期: 0 */5 * * * * *  ❌ 需要删除重建
   ```

5. **如果配置错误，需要重建触发器**：
   - 点击 "删除" 删除旧触发器
   - 点击 "新建触发器"
   - 触发器名称：`sendMatchReminders`
   - 触发周期：`*/5 * * * *`
   - 点击 "确定"

---

### 第三步：手动测试云函数

#### 在云开发控制台测试

1. **进入函数详情**
   - 云函数列表 → 点击 `sendMatchReminders`

2. **点击 "测试" 按钮**
   - 在函数详情页面右上角

3. **运行测试**
   - 测试参数：留空 `{}`（使用默认）
   - 点击 "运行测试"

4. **查看测试结果**
   
   **成功示例**：
   ```json
   {
     "success": true,
     "message": "没有需要提醒的比赛",
     "sentCount": 0
   }
   ```
   
   或（如果有比赛）：
   ```json
   {
     "success": true,
     "message": "成功发送 3 个比赛提醒",
     "sentCount": 3,
     "matchesProcessed": 1
   }
   ```

5. **查看执行日志**
   - 在 "日志" 标签页
   - 应该看到：
   ```
   开始执行比赛提醒推送任务
   查询时间范围: 2025-11-30 ~ 2025-11-30
   没有即将开始的比赛
   ```

#### 在微信开发者工具测试

1. **打开云开发控制台**（工具内）
   - 点击 "云开发" 按钮

2. **进入云函数**
   - 左侧 "云函数" 菜单

3. **找到并测试**
   - 找到 `sendMatchReminders`
   - 点击右侧 "测试" 图标
   - 查看返回结果

---

### 第四步：验证定时执行

#### 方法 1：查看日志（推荐）

1. **等待5分钟**
   - 定时器配置为每5分钟执行
   
2. **查看云函数日志**
   - 云开发控制台 → 云函数 → sendMatchReminders
   - 点击 "日志" 标签
   
3. **确认自动执行**
   - 应该看到每5分钟一次的自动调用记录
   - 调用方式显示：`timer` 或 `定时触发`
   
   **示例**：
   ```
   [2025-11-30 14:00:00] 开始执行比赛提醒推送任务
   [2025-11-30 14:05:00] 开始执行比赛提醒推送任务
   [2025-11-30 14:10:00] 开始执行比赛提醒推送任务
   ```

#### 方法 2：数据库检查

1. **打开数据库控制台**
   - 云开发 → 数据库

2. **查看 match_reminders 集合**
   - 如果有用户设置了提醒
   - 状态应该正确更新

---

### 第五步：端到端测试（完整流程）

#### 准备工作

1. **确保有测试数据**
   - 在小程序中打开 "欧洲赛程" 页面
   - 为某场即将开始的比赛设置提醒

2. **检查订阅消息权限**
   - 确保已授权订阅消息

#### 测试流程

1. **设置比赛提醒**
   ```
   小程序 → 欧洲赛程 → 选择比赛 → 点击铃铛图标
   ✅ 订阅消息授权
   ✅ 提示 "提醒设置成功"
   ```

2. **查看数据库记录**
   ```
   云开发控制台 → 数据库 → match_reminders
   应该有一条新记录：
   {
     userId: "xxx",
     matchId: "xxx",
     status: "active",
     createdAt: Date
   }
   ```

3. **等待云函数执行**
   - 等待定时器触发（最多5分钟）
   - 或手动在控制台测试云函数

4. **验证推送消息**
   - 如果比赛在30分钟内开始
   - 应该收到订阅消息推送

---

## 🔍 故障排查

### 问题 1：部署失败

**错误提示**：
```
上传失败：请检查网络连接
```

**解决方案**：
1. 检查网络连接
2. 确认云开发环境 ID 正确
3. 重新登录微信开发者工具
4. 尝试使用命令行部署

---

### 问题 2：触发器配置仍然错误

**现象**：控制台显示 7 位 cron 表达式

**解决方案**：
1. 删除旧的触发器
2. 手动创建新触发器
3. 配置为：`*/5 * * * *`
4. 确认保存

---

### 问题 3：云函数执行失败

**错误提示**：
```
errCode: -502001
function not found
```

**解决方案**：
1. 确认云函数已正确部署
2. 检查函数名称拼写
3. 重新上传云函数

---

### 问题 4：没有收到推送消息

**可能原因**：
1. 没有即将开始的比赛（30分钟内）
2. 用户未授权订阅消息
3. 订阅消息模板 ID 不正确

**排查步骤**：
```javascript
// 1. 检查是否有比赛数据
console.log('即将开始的比赛:', upcomingMatches);

// 2. 检查是否有提醒设置
console.log('提醒设置:', reminders.data);

// 3. 查看云函数日志
// 云开发控制台 → 云函数 → sendMatchReminders → 日志
```

---

## ✅ 测试检查清单

### 部署验证
- [ ] 云函数部署成功
- [ ] 无编译错误
- [ ] 控制台显示函数列表中有 sendMatchReminders

### 配置验证
- [ ] 触发器类型为 "定时触发器"
- [ ] Cron 表达式为 `*/5 * * * *`（5位）
- [ ] 触发器状态为 "已启用"

### 功能验证
- [ ] 手动测试成功（返回正确的 JSON）
- [ ] 日志输出正常（有 console.log 信息）
- [ ] 无运行时错误

### 定时执行验证
- [ ] 日志中看到自动执行记录
- [ ] 执行间隔约为 5 分钟
- [ ] 调用方式显示为 "timer"

### 端到端验证
- [ ] 可以设置比赛提醒
- [ ] 数据库中有提醒记录
- [ ] 收到订阅消息推送（如果有比赛）

---

## 📊 预期结果

### ✅ 成功标志

1. **部署成功**
   ```
   ✅ 云函数上传成功
   ✅ 依赖安装完成
   ✅ 触发器配置正确
   ```

2. **手动测试成功**
   ```json
   {
     "success": true,
     "message": "没有需要提醒的比赛" // 或 "成功发送 X 个比赛提醒"
   }
   ```

3. **日志正常**
   ```
   ✅ 开始执行比赛提醒推送任务
   ✅ 查询时间范围: ...
   ✅ 没有即将开始的比赛 // 或找到 X 场比赛
   ```

4. **定时执行正常**
   ```
   ✅ 每5分钟自动执行一次
   ✅ 日志中有 timer 调用记录
   ✅ 无执行错误
   ```

---

## 🎯 下一步

测试通过后：
1. ✅ 监控云函数运行日志
2. ✅ 观察用户反馈
3. ✅ 继续修复中优先级问题

测试失败时：
1. 📝 记录错误信息
2. 📝 查看详细日志
3. 📝 根据错误提示排查

---

## 📞 需要帮助？

如果测试过程中遇到问题：
1. 查看云函数日志获取详细错误信息
2. 检查网络连接和权限配置
3. 参考上面的故障排查部分
4. 记录错误截图和日志

---

**测试准备完成！现在可以开始测试了。** 🚀

建议按照顺序执行：
1. 第一步：部署云函数 ⭐
2. 第二步：验证控制台配置 ⭐
3. 第三步：手动测试 ⭐
4. 第四步：验证定时执行
5. 第五步：端到端测试
